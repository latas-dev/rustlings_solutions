// variables3.rs
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a hint.


fn main() {    
    println!("i32 max = {}\n\tas bits: {:b}", i32::MAX, i32::MAX);
    println!("i32 min = {}\n\tas bits: {:b}", i32::MIN, i32::MIN);

    println!("u32 max = {}\n\tas bits: {:b}", u32::MAX, u32::MAX);
    println!("u32 min = {}\n\tas bits: {:b}", u32::MIN, u32::MIN);

    // let x: i32 = ;
    // println!("Number {}", x);
}